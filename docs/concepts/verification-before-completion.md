# Verification Before Completion

This repo treats completion claims as a governed act, not a casual phrase.

A change can be:
- attempted
- partially implemented
- visible
- promising

And still **not verified**.

The tests in `runtime/tests/` check that non-passed verification states do not silently upgrade into success claims.
